info:
  name: Order History
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8082/api/orders/{{orderId}}/history
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("debe responder 200", function () {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
