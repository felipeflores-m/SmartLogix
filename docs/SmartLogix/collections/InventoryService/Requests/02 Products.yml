info:
  name: Products
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8081/api/inventory/products
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("debe responder 200", function () {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
