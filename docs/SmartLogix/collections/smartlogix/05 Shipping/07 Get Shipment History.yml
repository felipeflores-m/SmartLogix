info:
  name: "Get Shipment History"
  type: http
  seq: 7

http:
  method: GET
  url: "http://localhost:8080/api/shipping/shipments/{{shipmentId}}/history"
  body:
    type: none
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("debe responder 200", function () {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
