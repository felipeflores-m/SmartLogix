info:
  name: "Get Warehouses"
  type: http
  seq: 2

http:
  method: GET
  url: "http://localhost:8080/api/inventory/warehouses"
  body:
    type: none
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("debe responder 200", function () {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
