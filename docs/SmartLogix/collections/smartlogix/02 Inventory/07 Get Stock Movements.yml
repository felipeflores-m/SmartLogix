info:
  name: "Get Stock Movements"
  type: http
  seq: 4

http:
  method: GET
  url: "http://localhost:8080/api/inventory/stock/movements"
  body:
    type: none
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("debe responder 200", function () {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
