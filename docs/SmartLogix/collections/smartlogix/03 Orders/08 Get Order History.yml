info:
  name: "Get Order History"
  type: http
  seq: 5

http:
  method: GET
  url: "http://localhost:8080/api/orders/{{orderId}}/history"
  body:
    type: none
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("debe responder 200", function () {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
