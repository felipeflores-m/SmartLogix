info:
  name: "Me Directo"
  type: http
  seq: 5

http:
  method: GET
  url: "http://localhost:8084/api/auth/me"
  body:
    type: none
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("debe responder 200", function () {
          expect(res.status).to.equal(200);
        });

settings:
  encodeUrl: true
