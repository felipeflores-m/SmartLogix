info:
  name: "Get Customer By Id"
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/orders/customers/{{customerId}}"
  body:
    type: none
  auth: none
  headers:
    - name: "Authorization"
      value: "Bearer {{token}}"
      disabled: false

settings:
  encodeUrl: true
