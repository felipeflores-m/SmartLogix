info:
  name: "Get Order By Id"
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/orders/{{orderId}}"
  body:
    type: none
  auth: none
  headers:
    - name: "Authorization"
      value: "Bearer {{token}}"
      disabled: false

settings:
  encodeUrl: true
