info:
  name: "Get Product By Id"
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/inventory/products/{{productId}}"
  body:
    type: none
  auth: none
  headers:
    - name: "Authorization"
      value: "Bearer {{token}}"
      disabled: false

settings:
  encodeUrl: true
